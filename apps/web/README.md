# Web App

Interactive browser UI for generating block JSON through `blockGenerator`
WebAssembly and rendering generated blocks with `blockRenderer`.

## Usage

```powershell
npm.cmd start
```

The start script builds the generator WebAssembly, serves `apps/web/public`, and
prints a local URL.

## Tests

```powershell
npm.cmd test
```
