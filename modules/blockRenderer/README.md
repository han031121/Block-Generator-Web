# blockRenderer

Initial console-based loader for generated block JSON data.

## Usage

```powershell
npm start
```

Optional arguments:

```powershell
node src/main.js [blockJsonPath] [blockIndex]
```

If no JSON path is provided, the renderer reads:

```text
../blockGenerator/fixtures/output/test_output.json
```

